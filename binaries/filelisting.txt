ls_default1: configured, compiled with standard coreutils Makefile
	ls_default1_recompile: 'make clean, make' (not re-running ./configure)
ls_default2: configured, compiled with standard coreutils Makefile
	ls_default2_recompile: 'make clean, make' (not re-running ./configure)
ls_default3: configured, compiled with standard coreutils Makefile
	ls_default3_recompile: 'make clean, make' (not re-running ./configure)


rm_default1: same configuration as ls_default1, compiled with standard coreutils Makefile
rm_default2: same configuration as ls_default2, compiled with standard coreutils Makefile
rm_default3: same configuration as ls_default3, compiled with standard coreutils Makefile

